#pragma once

namespace Intimate
{
    struct AnimationData;
    class Engine;

    namespace Animations
    {
        void blank(Engine &engine, AnimationData &data);
        void glitter(Engine &engine, AnimationData &data);
    }
}